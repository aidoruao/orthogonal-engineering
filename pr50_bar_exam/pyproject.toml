[project]
name = "pr50_bar_exam"
version = "50.0.0"
description = "Bar Exam — Ordination for Architectural Stewards"
requires-python = ">=3.9"
dependencies = [
    "jsonschema>=4.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]
