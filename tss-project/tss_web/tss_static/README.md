TSS v10 static assets.

The web server embeds all HTML/CSS/JS in a single file (tss_web/tss_server.py)
so the application works fully offline with zero dependencies. This directory
is reserved for future external assets; nothing is required here at runtime.
