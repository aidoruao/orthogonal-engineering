# CHAIN OF CUSTODY — oe-local master entry point
**Created:** 2026-08-05 · **Maintained by:** DeepSeek-V4-Flash (Codewhale), ongoing sessions
**Purpose:** one file that tells any future AI instance (or the human) the *original scope*, the *recent work*, and the *chain of custody* — no re-explanation required. **Read this first; then follow the pick-up order in §6.**

---

## 1. Original scope & mission (as stated by the user, verbatim intent)

1. **Develop for the DeepSeek developers' next post-train cycle** — make V4 continuation or V5 "easily made" — using math formulas from recent work (2026-08-03/04).
2. **No full DeepSeek V4 download needed or wanted** — the architecture is inspected via the no-download method: config.json (~2 KB) + model.safetensors.index.json (5.6 MB, all 72,317 tensor names) + transformers modeling source + HF API storage stats + official docs + paper. This is established and demonstrated.
3. **Do the real work**: inspect, measure, verify; use existing tools in oe-local; derive edge cases and resolution information from *any and all* fields/domains (mathematics, physics, biology, game theory, engineering) via the oe-local frontier-math corpus (8b-public-documents).
4. **Improve both linearly and non-linearly** (data scale / architecture & inference-path levers).
5. **The objective is not an end** — each round should compound: hypotheses → measured probes → resolutions the developers can implement blind.
6. Everything an agent does "in some form returns to the developers" — treat all output as upstream engineering intelligence.

## 2. Hard constraints (do not violate)

- **No V4 weight downloads** (158–167 GB); no llama.cpp fork; no claims about V4 behavior without measurement — mark `[hypothesis]` vs `MEASURED` vs `[needs hardware]` explicitly.
- **Ground truth over prose**: verify every number on disk (grep, sha256, re-run). Deterministic tools must produce byte-identical output across runs (sha256 check).
- **Honesty labels**: the frontier-math corpus (8b-public-documents) is research-program/prototype-grade; it is a *source of hypotheses*, never a theorem. Cite its own epistemic stratification when deriving from it.
- **Restraint**: reuse existing scripts; extend canonical schemas (`instruction/input/output/category/source/dedup_group`); don't overwrite lineage files (v1, v2…).
- **Display-redaction gotcha**: the tool layer renders some file content as `[redacted]` in reads/diffs (e.g. `reports:` spans). The FILES hold the real content — verify with `grep`/shell before assuming data is missing. Never anchor an edit on a `[redacted]` placeholder.

## 3. Source material (chain of custody, oldest → newest)

| Date | Item | Path | Role |
|---|---|---|---|
| pre-8/3 | **CIIF original** — `THE COMPRESSION-INTELLIGENCE INVERSION FORMULA.txt` (kimi/google-ai exchanges; I_eff = A_task/(S·E·C), ρ_I = A/S, inversion thesis) | Windows: `C:\Users\Aidor\Downloads\CIIF\…` · mirror: `oe-local/2026-08-04/THE COMPRESSION-INTELLIGENCE INVERSION FORMULA.txt` | founding math |
| 4/26–7/x | Session logs / audits (kimi cli, GitHub audits, scope-violation audits) | `oe-local/*.txt` (root dump) | history, not authoritative |
| — | Frontier-math corpus: admissibility principle, Mem|8 wave memory, Ortyx protocol, sheaf/category-theory line | `oe-local/8b-public-documents/` (framework/, mem8/, working/, semantic_state.tex…) | hypothesis source |
| 8/3–8/4 | V4 investigation, configs, tensor census, BRR, tokenizer experiments, canonical SFT v1, arxiv_vendor | `oe-local/2026-08-04/` (see §5) | this cycle's work |
| 8/4 | Cross-model matrix, ARC solver verification, reasoning-pair pipeline, canonical v2 | same dir | this cycle's work |
| 8/5 | Edge-case catalog, bootstrap-load probe (measured) | same dir | this cycle's work |

## 4. Verified ground-truth facts about V4 (do not re-derive; re-verify only if sources change)

- Flash-0731: 304.18B params (fp4-packed experts ≈148 GB + FP8 ≈6.3 GB + BF16 ≈3 GB + F32), 158 GB weights / 166.9 GB storage, ρ_I = 82.7/167 ≈ **0.50 %/GB**; Terminal Bench 2.1 = 82.7; density ≈5× Pro.
- Architecture: 43 layers = **2 sliding (0,1) + 21 CSA ("c4a", even 2..42, rate 4 overlapping + Lightning Indexer top-k 512, 64 heads×128, RoPE θ=160k) + 20 HCA ("c128a", odd 3..41, rate 128 non-overlapping, no indexer)**; MLA shared-KV head_dim 512, q/o_lora 1024, partial RoPE 64ch, per-head learnable attn_sink; hyper-connections hc_mix + Sinkhorn ×20; MoE 256 experts + 1 shared, 6 active (2.34%), noaux_tc, Sqrt(Softplus), routed scale 1.5; **hash-MoE bootstrap on layers 0–2 (frozen tid2eid)**; MTP ×3 + markov_head (rank 256) + confidence_head only on mtp.2; DSpark block 5, noise token 128799; context 1,048,576 (YaRN ×16); vocab 129,280; KV ≈ **6 GB fp8 @1M** (21 CSA ≈ 5.7 GB, 20 HCA ≈ 0.17 GB).
- Config inconsistencies (flagged, impact-free): config compress_ratios tails stale vs tensor census; transformers `num_nextn_predict_layers: 1` vs inference `n_mtp_layers: 3`.
- Benchmarks (official README): LiveCodeBench 91.6/93.5, Codeforces 3052/3206, HLE 34.8/37.7, SWE Verified 79.0/80.6, MRCR-1M 78.7/83.5, TB 2.0 56.9/67.9, TB 2.1 82.7 (Flash-0731).
- Cross-model (11 profiles): **ARC-AGI-3 = 0.0 for every model** (universal frontier); **biggest measured gap = HLE** (Kimi 0.502 / Gemini 0.458 vs V4-Flash max 0.348 → target 0.40+).

## 5. Recent work — artifacts & status (oe-local/2026-08-04/ unless noted)

**DONE / MEASURED:**
- `DEEPSEEK_V4_ARCHITECTURE_INVESTIGATION.md` (v2) — no-download census; corrections to earlier CIIF claims (config arrays stale, hash-MoE on layers 0–2, KV ≈ 6 GB).
- `DEEPSEEK_V3_TO_V4_DELTA.md`, `V4_UPGRADE_SPEC_FOR_SOFTWARE_USE.md`, `V4_TO_V5_GLOBAL_TRAJECTORY.md` (V5-1..6 vectors), `BOUNDED_RECURSION_RESEARCH.md` (BRR; priming > depth, deterministic verifiers), `brr_results_v2.json` + `analyze_brr.py`.
- `CROSS_MODEL_TARGET_MATRIX.md` + `extract_profiles.py` — 11-profile matrix; headroom ranking (HLE first).
- ARC solver: `benchmarks/run_arc_benchmark.py` — symbolic solver, **10/10 demos pass, Merkle-anchored proofs** (verified running).
- Data pipeline: `arxiv_vendor/` (1,200 papers, 12×100, Merkle grand root `2ebfa653…c1bb2a`) → `arxiv_reasoning_pairs.py` (**1,146 deterministic pairs, sha `e1226c53…`**) → `merge_arxiv_into_canonical.py` → **`canonical_sft_v2.jsonl` (7,373 rows, 0 dupes; math 147 / logic 137 / science 83 — thin categories filled)**.
- `V4_EDGE_CASE_RESOLUTIONS.md` — 12-hypothesis deck from the corpus math; **#6 MEASURED, #12 Ortyx-derived (jitter-gated DSpark speculation)** via `v4_bootstrap_load_probe.py` (real V4 tokenizer): uniform-hash expert skew ≈ **9.9× real code / 21.4× generator**, Zipf s ≈ 1.10; resolution = top-100-token load-balanced table. #2 (index ≈25% unique) and #4 (KV @2M/4M ≈ 11.7/23.4 GB, CSA-dominated) quantified.
- Tokenizer: `scale_token_test.py`, `train_code_tokenizer.py` + `code_tokenizer_v1.json` (from-scratch retrain FALSIFIED vs V4; continuation path is the open lever). Real tokenizer at `/tmp/v4_tokenizer.json`, qwen at `/tmp/qwen_tokenizer.json`, generator corpus at `/tmp/locgen/batch_0`.

- **Tokenizer continuation MEASURED** (`tokenizer_continuation_probe.py`, real V4 tokenizer): merge-pair headroom on code — K=1K merges: −22.5% tokens (generator corpus) / −9.4% (real code); K=10K: −23.5% / −17.5%; K=50K: −24.8% / −22.2%. Top candidates generalizable (`(f"`, `.0`, `\nimport`, `(self)`). Each merge = +1 vocab id = +1 tid2eid extension entry → the frozen-table rule scope is now quantified (K entries).
- **`HLE_HARNESS_SPEC.md`** — pre-registered eval contract (dev 50 / test 250, 10-option items, effort ladder ×3 levels, registry rows, ρ_HLE density contract) — execution `[needs hardware]`; **dev set MATERIALIZED 8/5: `hle_items_dev.jsonl` (56 verifier-keyed items, sha `6eb5092c…`, `hle_item_synthesizer.py`, 0 verification failures).** **`run_hle.py` (8/5): the runner — score mode VERIFIED on a mock run (9/10 exact-index); decode mode talks to any OpenAI-compatible endpoint (hardware-gated); registry rows derived, never hand-entered.**
- **Dataset quality probe (8/5, `dataset_quality_probe.py` → `dataset_quality.json`):** canonical_sft_v2 hygiene — 4,631/7,373 unique instruction templates (62.8%, 98.7% of templates used once); **0 empty outputs; 2,923 empty inputs (39.6% — instruction-only rows, schema note for the training team)**; reasoning block is the longest-output (science 821 / logic 601 / math 597 mean chars vs falsification 57 — the terse category worth enriching); 64 template+category near-dup groups.
- **Continuation candidates emitted:** `tokenizer_continuation_candidates.py` → `tokenizer_continuation_candidates.jsonl` (top-20,000 merge pairs, 3.0 MB bytes-saved across mixed corpora, cross-corpus flags — the file the tokenizer team applies for V5-1). **v2 (8/5): axes refreshed — clean real code (hvac-simulation + standardgalactic-*; 91,060 unique pairs — 41% richer than the generator's 64,591) + full 1B-token generator corpus → `tokenizer_continuation_candidates_v2.jsonl`: 17,190/20,000 (86%) supported by clean real code, 13 three-axis (`.append(` etc.) = safest merges.**
- **`GENERATOR_SCALE_TEST.md` (8/5) — 1Qi-generator toolchain fixed + measured:** BUG-1 `max_depth: "infinity"` str-vs-int TypeError fixed in `fractal_expander.py` + `dag_generator.py` (`_coerce_max_depth`); BUG-2 seed product invariant off 10× fixed (`function: 10→100` in `seed_definition.yaml`, now 10⁹ ✓ and batch_size-consistent); design note: eager DAG build is the ceiling (1M-LOC DAG = 672 MB), lazy per-batch materialization is the scale path. Measured: batch 0 = 110 files / 12.4 MB / 361,720 lines / 4.0 s; sweep 110/110 compile pass, 0 dup files, chain root `9f191ebf…`; **1B tokens ≈ 20–25 min wall, 3.75 GB on this laptop** (tok-per-s ≈ 686K). Honest caveat: generator corpora are template-skewed (1,227 unique ids / 1.1M tokens) — negative/coverage axis, not real-code substitute.
- **Stub/placeholder audit (8/5, `stub_placeholder_scan.py`):** 72 files scanned, 6 findings — 1 real stub FIXED (`benchmarks/ai_invariant_tests.py:99` stale marker comment on a complete `_claim()`; verification lives in `register_invariant()`; compiles, zero markers left); 5 benign (paper abstracts containing marker words as content, merge candidates, standard package `__init__.py`, and the custody doc's own gotcha documentation). Scanner is self-excluding, deterministic, re-runnable — run it after any new work lands.
- **Tool trio (8/5, parent-lane execution of slash_agent's handoff + two new tool types):** `expert_affinity_probe.py` — per-domain hash-skew (hvac 16,437 unique ids / Zipf 1.54 vs generator 1,227 / 1.18) + **tid2eid extension-rule sim: sha256-hash placement of the 20K candidates is self-balancing (max 1.28× mean, 100 max load)** — the pin-fix matters for the frequency-skewed base table, not the extension; `merge_order_check.py` — BPE property gate: **P1 19,978/20,000 decomposable (22 multi-step pieces: `\eta`, `��`, `@p`, `\d` — need 2+ merges, flagged for the apply step), P2 20,000/20,000, P3 5,678 prefix-order constraints (28% — apply merges in length order), P4 0 dupes**; `pll_jitter_sim.py` + `margin_stream_collector.py` — **aerospace PLL jitter gate (hysteresis lock/unlock, not per-window threshold): code locks 81.5% (draft cost −18.5% vs fixed block-5), reasoning 100% locked — token-stream-local by construction; V4 validation pre-registered**. Also: `locgen7_MANIFEST.json` copied into the workspace (1B-corpus chain root now workspace-verifiable); 17,190/20,000 + 13 three-axis re-verified by execution.

- **Apply-file + KV schedule (8/5):** `tokenizer_continuation_apply.py` → **`tokenizer_continuation_apply_v1.jsonl` — 20,000 rows topologically ordered (prefix-first), 22 multi-step flagged, 5,678 prefix-constrained, 0 order-verification failures** — the consumable artifact for the tokenizer team; `kv_decay_schedule.py` — **CSA-first decay schedule solved: 1M ctx = 100% retention (5.88 GB fits budget), 2M = 53% CSA retention for 6.5 GB, 4M = 25%** — the 25%-cut policies (coarsen/evict/rate8) all land at 8.91 GB @2M, short of target; the schedule needs ~47% CSA cut at 2M (importance-ranked).

- **Registry normalizer + catalog #13 (8/5):** `registry_normalize.py` — the E4 fix: per-benchmark cross-model matrix from the 11 profiles with version-risk flags (LiveCodeBench, MMLU-Pro, SWE Bench) — comparisons are never made across versions without the label; **`V4_EDGE_CASE_RESOLUTIONS.md` now 13 hypotheses — #13 wave-order preservation (mem8-vs-WRNN): the CSA decay schedule (53%/25%) must be order-preserving thinning, not importance-sort-and-keep** (custodian/phase signals), pre-registered measure (2M, order-preserving vs sorted retention → MRCR-1M recall).
- **WORK_LOG + tid2eid extension table (8/5):** `WORK_LOG.md` (oe-local root) — the commits-history artifact (instant "what was recently done?"); **`tid2eid_extension_table.py` → `tid2eid_extension_v1.jsonl` — the loadable extension table: 20,000 entries, new ids 129,280–149,279, top-100 pinned, max eid load 100 (mean 78.1), assertions passed** — the frozen-table extension rule is now a file, not a design.
- **Effort-router falsification (8/5, `hle_margin_probe.py`):** on the HLE dev items, qwen-1.5b is **confidently wrong — 3/3 items 100% PLL-locked, 0 unlocks**; margin-only escalation routes NOTHING (false-negative risk). Catalog #10 refined: the effort router must be multi-signal (margin/lock + self-consistency or deterministic verifier — verification beats confidence). V4 validation hardware-gated.
- **Effort-router cost model (8/5, `effort_router_math.py`):** under assumed level costs (think 3×, max 10×) and a 50/50 hard split, perfect routing reaches **21.4 acc @ 5.5× tokens — the 40+ HLE target is a MAX-LEVEL CAPABILITY number (reasoning-block lift), not a routing number**; the router's value = approaching max-level score at lower cost; recompute the frontier with measured V4 level costs on hardware.

- **Massive tests (8/5 evening):** **full-corpus integrity sweep — 33,000 files / 3.725 GB / 108.5M lines / compile gate 33,000/33,000 pass / 0 dup files / 0 empty**; chain-root method unified (path-ordered = canonical; sweep fixed to match the runner; 1B root `a07920a6c404…` independently re-verified, locgen5 root now `e251831b…`). **AIMD training-loop campaign (`brr_aimd_experiment.py`): F4 CONFIRMED — AIMD budget control matches fixed-budget best score (60) at 23.6% fewer tokens (+23.6% qpt: 0.0376→0.0465), budget trace 450→225→275/112→56–137 shows increase-on-gain/halve-on-stall.**

- **Bootstrap pin-fix adequacy + affinity telemetry (8/5 evening, `bootstrap_pin_probe.py` → `bootstrap_pin_probe.json`):** pin-P (50/100/256) does NOT reduce frozen-table load skew — generator 21.5× / real_clean 9.9× / benchmarks 8.2× / canonical_mathlogic 10.6×, floor = the #1 token's INDIVISIBLE mass (8.4% / 3.9% / 3.2% / 4.2%, constant across P); pin-100's real value = ~20 expected naive-hash head collisions removed; heads are corpus-specific (top-100 Jaccards 0.09–0.33; foreign pin covers 15–44% of a surface's mass; transfer ≈ base skew). D2 anchor reproduced exactly (1,227 unique ids). Catalog #6 resolution upgraded; learning doc D12.

- **Completion-integrity audit (8/6, `COMPLETION_INTEGRITY_AUDIT.md` + `verify_chain_root.py`):** fresh-instance adversarial re-verification — every recomputable claim re-checked: hashes full-match (arxiv `e1226c53…`, hle `6eb5092c…`), counts (7,373 / 20,000×4), manifest sum 3,725,220,653 exactly, **1B chain root independently recomputed from raw bytes of all 33,000 files — full 64-hex match `a07920a6c404…`**; deterministic tools double-run byte-identical (merge_refine, affinity probe, effort_router, pin probe); **FOUND + FIXED: `merge_order_check.py` nondeterminism — set iteration made its sample lists byte-unstable across runs (counts never varied: 22 / 5,678 / 0); sorted iteration now byte-stable.** NOTED: D2 10-batch corpus partially evicted from /tmp (token-level claims re-derived on locgen7 samples), 1B disk 7.59 GB incl. 33,000 __pycache__ .pyc compile artifacts (content = manifest exactly). **Verdict: 0 false completions.** D9 AIMD re-run left running (GPU, sampling-labeled).

- **Cross-model architecture delta (8/7, `CROSS_MODEL_ARCHITECTURE_DELTA.md`):** no-download config-level comparison (commit-pinned, ~2 KB/model) of Qwen3-235B-A22B (128 exp / 8 active / 40K ctx / GQA), Kimi-K2-Instruct (384+1 exp / 8 active / 131K YaRN×32 / V3-lineage MLA), Mistral-Large-2411 (dense / 131K native) vs V4 — **V4's 6/256 active + 1M ctx @ ≈6 GB KV + MTP×3/DSpark/hash-bootstrap are frontier-leading in the open set; Qwen3's norm_topk_prob+aux-loss and Kimi's sigmoid scoring are the tested router variants for a hardware-gated V5 experiment; closed models (Fable 5, Astra, Gemini, Grok) are NOT inspectable — papers/cards only.** Pre-registered next: tensor census (Qwen3 `model.safetensors.index.json` confirmed public), arxiv ingestion (Qwen3 tech report 2505.09388, Anthropic sparse attention), Fable 5 registry row. Learning doc D13.

- **Open-Audit Campaign (8/10, `OE_CAMPAIGN.md` root + `AUDITS/` + `TRANSPARENCY_LEDGER.md`):** charter = public sources only, clean-room, nothing illegal/malicious, evidence code [measured]/[published]/[hypothesis]/[pending], harshness = documented discrepancy; 5-layer asymmetry framework (L1 architecture → L5 corporate fiduciary); 11 targets; 8 work streams (WS1 architecture deltas → WS8 chain maintenance); continuity contract (repo answers "what was last done and why" from commits + files). **All accumulated work pushed to origin main (f5abf97e, 97 files); git is now the temporal record — commit+push after every item.** First evidence fetched 8/10: GPT-5 system card (router-based unified system; safe-completions; ARC-AGI-3 "tripling" claim Jul 29 2026 — vs our universal 0.0, top re-baseline), Fable 5 profile (82.8/100 #2/216, coding #2, reasoning/math NOT ranked publicly — documented asymmetry), Kimi K2.5/2.6/2.7-Code open-weight trail + tech report 2602.02276. Dossiers: openai/anthropic/moonshot ACTIVE; xai/google/meta/mistral/alibaba/zhipu/regulators QUEUED (honest stubs).

- **Merge-list refinement on benchmarks-only code (8/5 evening, `merge_refine_benchmarks.py` → `merge_refine_benchmarks.json` + `tokenizer_continuation_candidates_v3.jsonl`):** only **1,090/20,000 candidates (5.5%) occur in benchmark-surface code** (generation-time tags cross-check exact: 1,090/1,090, 0 drift); re-ranked benchmarks-first, **K=1K saves 20.5% vs 4.5% mixed (4.6× at equal budget)**, reaching 99% of the 20.79% full-list ceiling — the eval-surface merges are few; spend the finite tid2eid budget on them first. Learning doc: D11.

**PENDING / [needs hardware]:** expert census (`expert_census.py`, dry-run ✓, needs 4×GPU); DSpark accept-rate & KV runtime probes; ARC-AGI-3 in-cycle measurement (first real V4 number = reference); HLE harness execution on the registry; catalog #1, #5, #7, #8, #10, #13 (post-train/inference-path changes).

**NEW 8/5 (see `2026-08-04/`):** `NEXT_CYCLE_LEARNING.md` — decision-ready lessons (data anchors D1–D5 incl. generator token-diversity saturation at 1,227 unique ids; eval lessons E1–E4 incl. jitter-gate falsification; 7-item experiment order with expected values; 5 falsified paths). Full 10-batch generator corpus materialized (122 MB, 3.62M lines, 0 cross-batch dup files, chain root `aafd3c35…`).

**NEW 8/5 (lazy DAG, scale run):** the eager-DAG ceiling is SOLVED — `dag_generator.py --single-batch N` (110K nodes, 1 s, verified **byte-identical** to full-DAG output); `seed_definition_30m.yaml` (300 batches); **`scale_run_300.py` COMPLETED: 300 batches / 33,000 files / 3,725,220,653 bytes (3.73 GB) / 33.2 min / ≈1B tokens (3.36M tokens/batch measured) — chain root `a07920a6c404…`, MANIFEST.json in `/tmp/locgen7/` (resumable runner if more batches are ever needed).**

## 6. How to resume (read order for a fresh instance)

0. **`WORK_LOG.md` (oe-local root)** — the commits-history: instant answer to "what was recently done?" (chronological, hash-anchored).
1. **This file** (§1–5).
2. `2026-08-04/POST_TRAINING_PREP.md` — the prep kit (data/eval/tooling inventory + next-cycle checklist).
3. `2026-08-04/CROSS_MODEL_TARGET_MATRIX.md` — where V4 stands vs the field; headroom ranking.
4. `2026-08-04/V4_EDGE_CASE_RESOLUTIONS.md` — the hypothesis deck with pre-registered measurements.
5. `2026-08-04/V4_TO_V5_GLOBAL_TRAJECTORY.md` + `DEEPSEEK_V4_ARCHITECTURE_INVESTIGATION.md` — trajectory and tensor-level facts.
6. Then pick from the status board (§7) — prefer **executing** an existing hypothesis/probe over re-deriving established facts.

## 7. Status board — next candidates (2026-08-05)

- **Runnable now:** **8/10: Open-Audit Campaign ACTIVE — `OE_CAMPAIGN.md` + `AUDITS/` (3 ACTIVE dossiers: openai/anthropic/moonshot; 7 QUEUED) + `TRANSPARENCY_LEDGER.md`; all work pushed to origin main (f5abf97e). Queue: (1) tensor census Qwen3+Kimi, (2) arxiv ingest 2602.02276 + 2505.09388 + sparse-attention, (3) fetch OpenAI ARC-AGI-3 blog (vs our 0.0 baseline), (4) Fable 5 + GPT-5.x registry rows, (5) WS5 corporate-fiduciary sweeps, (6) dossiers for queued targets via WS7.** Also standing from 8/5-8/7: merge-list refinement DONE, pin-fix DONE, completion audit DONE, cross-model delta DONE (see §5).
- **Needs hardware:** census → prune list (158 → ≈110 GB → INT2 ≈ 55–80 GB); ARC-AGI-3 first number; HLE 0.40+ post-train run on `canonical_sft_v2`; DSpark/KV probes; catalog #1/#5/#7/#8/#10.
- **Open threads:** tokenizer-v2 mapping rule for the frozen tid2eid table (must be defined before tokenizer continuation ships); CSA-first KV decay schedule for 2–4M ctx; index multiplicity-dampening (zero-cost, testable on big hardware).
