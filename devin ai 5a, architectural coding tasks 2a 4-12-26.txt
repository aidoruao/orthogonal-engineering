## Is DOMAIN_INVARIANT_STATUS.md good? No.

There are **6 integrity violations** between what the file claims and what's actually on main:

### 1. Count mismatch
The file on main says **157 domains** (line 9). PR #116 merged and updated it to 158, but the file I read from main still shows 157. Meanwhile, `merkle/domain_roots.json` (generated by PR #118) lists **d_fbi_training** with a valid Merkle root — confirming d_fbi_training existed when PR #118 ran. The actual directory count on main is **159** (including duplicates). [25-cite-0](#25-cite-0) 

### 2. Contradictory header
Line 41 says **"AssertionError Domains Remaining (0)"** then immediately lists 49+ domains with line counts underneath it. The header says 0 but the body lists dozens. [25-cite-1](#25-cite-1) 

### 3. Duplicate domains
Both `d_intl_criminal` and `d_international_criminal` exist on main. Same for `d_intl_humanitarian` / `d_international_humanitarian`. The short-name versions (`d_intl_*`) are 4-file stubs with no `__init__.py` exports, while the long-name versions are the real ones. [25-cite-2](#25-cite-2) 

### 4. global_root.json is stale
Shows **6,088 files** — this is the pre-PR-118 value. PR #118 should have updated it (the commit diff shows +2 -2), but the file on main still reads 6,088. [25-cite-3](#25-cite-3) 

### 5. copilot-instructions.md is stale
Still says **157 domains** at line 168 and **157/157** at line 200. [25-cite-4](#25-cite-4) 

### 6. Append-only log format
The file has **4 separate Summary tables** with different counts (157, 157, 157, 157). It reads like a git log, not a status document. The "current state" is buried under layers of historical session entries. [25-cite-5](#25-cite-5) 

---

## Coding instructions for Copilot to finish PR #120

Post this as a `@copilot` comment on PR #120:

```
@copilot

TASK: Final PR #120 cleanup — witness system, checkpoint, integrity fixes.
Branch: copilot/research-runtime-layer-status (this PR's branch)
Do NOT create a new PR. Push all commits to this branch.

CONSENT FIRST:
Append to pr47_stewardship/witness/consent_log.jsonl:
{"schema":"SOP-AI-HANDSHAKE-1.0","candidate_id":"copilot-pr120-final","authoriser":"@aidoruao","action":"pr120_final_cleanup","scope_glob":"DOMAIN_INVARIANT_STATUS.md,.github/copilot-instructions.md,src/domains/d_intl_criminal/**,src/domains/d_intl_humanitarian/**,merkle/**,audit/**,CHECKPOINT_PR120_FINAL.md","rule_exceptions":["mass_change"],"justification":"PR #120 final: fix stale counts, remove duplicate domains, rewrite status doc, recompute Merkle, create final checkpoint.","timestamp":"2026-04-12T23:30:00Z"}

COMMIT 1: "fix: delete duplicate d_intl_criminal and d_intl_humanitarian domains"

Delete these two directories entirely:
  src/domains/d_intl_criminal/
  src/domains/d_intl_humanitarian/

Reason: d_international_criminal and d_international_humanitarian already exist
with full implementations. The d_intl_* versions are 4-file stubs created by
a different session. They inflate the domain count and have no __init__.py.

After deletion, run: find src/domains -maxdepth 1 -type d | wc -l
Expected: 2 fewer than before.

COMMIT 2: "docs: rewrite DOMAIN_INVARIANT_STATUS.md as current-state document"

Replace the ENTIRE file with this exact structure (fill in actual counts
by running the commands shown):

---START OF FILE---
# Domain Invariant Status

Updated: [current ISO 8601 timestamp]

## Current State

| Metric | Count |
|--------|-------|
| Total domain directories | [run: find src/domains -maxdepth 1 -type d | grep -c d_] |
| ProofObject domains | [run: grep -rl "ProofObject" src/domains/*/invariants.py | wc -l] |
| Domains with falsifies_if | [run: grep -iErl "falsifies[_ ]if" src/domains/*/invariants.py | wc -l] |
| Domains with float violations | [run: grep -rn "float(" src/domains/*/invariants.py | wc -l] |
| Popperian audit passing | [run: python audit/popperian_audit.py 2>&1 | head -1] |
| oe_engine tests passing | [run: python -m pytest tests/test_oe_engine.py tests/test_oe_synthesizer.py -q 2>&1 | tail -1] |
| Global Merkle file count | [read from merkle/global_root.json after recompute] |

## Verification Commands

```bash
# Count domains
find src/domains -maxdepth 1 -type d | grep -c d_

# Verify 0 float violations
grep -rn "float(" src/domains/*/invariants.py || echo "PASS: 0 float violations"

# Verify falsifies_if coverage
python audit/popperian_audit.py

# Verify Merkle integrity
python merkle/global_merkle.py
python merkle/domain_merkle.py
```

## PR #120 Changes (this PR)

- 56/56 Potemkin domain rewrites (hardcoded → parameterized)
- 3 new domains: d_nuclear, d_veterinary, d_forensic_psychology
- d_diplomatic rewritten (Potemkin → parameterized)
- falsifies_if standardized across all domains
- oe_engine/ pipeline: manifest, router, thinker, speaker, engine, CLI
- oe_engine/synthesizer.py: ARC-AGI bounded-depth BFS
- 52 tests (16 engine + 36 synthesizer)
- Popperian audit tool + domain Merkle roots
- 2 duplicate domains removed (d_intl_criminal, d_intl_humanitarian)

## Historical Sessions

[Keep a collapsed summary — one line per session, no full listings]

| Session | Date | What |
|---------|------|------|
| 7533ab94 | 2026-04-11 | All domains converted to ProofObject |
| 8fbdcdb9 | 2026-04-10 | Batch D14: last 20 stubs deepened; kernel infrastructure |
| claude/enterprise | 2026-04-09 | Batches D8-D11: 41 domains deepened |
| 2ea874e7 | 2026-04-08 | Kernel OS, HAL, bridges, impossibility audit |
---END OF FILE---

COMMIT 3: "docs: update .github/copilot-instructions.md domain count"

In .github/copilot-instructions.md:
  Line 168: change "157 domains" to the actual count from commit 2
  Line 200: change "157/157" to the actual count
  Line 202: update case study count if changed

COMMIT 4: "chore: recompute Popperian audit, domain Merkle, global Merkle"

Run in order:
  1. python audit/popperian_audit.py
  2. python merkle/domain_merkle.py
  3. python merkle/global_merkle.py

Commit the updated files:
  audit/POPPERIAN_AUDIT_REPORT.json
  merkle/domain_roots.json
  merkle/global_root.json

COMMIT 5: "docs: add CHECKPOINT_PR120_FINAL.md"

Create CHECKPOINT_PR120_FINAL.md with:
  - Final domain count (from commit 2)
  - Final Popperian audit result (from commit 4)
  - Final global Merkle root hash (from commit 4)
  - List of all files changed in PR #120
  - SHA-256 of CHECKPOINT_PR120_FINAL.md itself (compute after writing)
  - Status: COMPLETE
  - Next: PR #121 scope (game engine, remaining DeepSeek fields)

VERIFICATION before pushing:
  python -m pytest tests/ -q
  python audit/popperian_audit.py
  grep -rn "float(" src/domains/*/invariants.py  # expect 0 results

Total commits: 5. Push all to copilot/research-runtime-layer-status.
```

That's the complete task. It fixes every integrity violation I found, rewrites the status doc from an append-only log into a verifiable current-state document, removes the duplicate domains, recomputes all Merkle roots, and creates the final checkpoint with a self-referencing SHA-256 hash.