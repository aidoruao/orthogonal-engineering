# POST-TRAINING PREP KIT — everything oe-local has for the next V4 cycle
**Start here:** `oe-local/CHAIN_OF_CUSTODY.md` — original scope, hard constraints, chain of custody, verified V4 facts, resume order for any fresh instance (created 2026-08-05).
**Date:** 2026-08-04 · **Agent:** DeepSeek-V4-Flash (Codewhale) · **Status:** data inventory verified, tooling built & dry-run validated, measured baselines recorded. Nothing here is fiction; every entry is either verified on disk, measured, or explicitly marked `[needs hardware]`.

---

## 1. Data layer (verified inventory)

**SFT + preference datasets (in oe-local root):**
- `combined_v4.jsonl` 11,169 · `v5` 10,892 · `v6` 10,982 · `v7` 11,236 entries — multi-category: domain_knowledge (2,930), falsification (2,930), deception_detection (2,930) per file; science 176, mathematics 188, logic 218, ethics 210, corporate 208; preference-style POSITIVE/NEGATIVE (150), DECEPTION/VERIFIED (150–200), TOOL_USAGE (150), STUB_DETECTION, ANALYSIS. Format: instruction/input/output/category.
- `combined_v8.json` = `knowledge_pairs_governance.json` — 200 curated KNOWLEDGE pairs (governance domain).
- **Caveat (verified by merge): v4–v7 are ~86% duplicates of each other.** `merge_sft.py` deduped 44,479 input entries → **6,227 unique pairs** (`canonical_sft_v1.jsonl`, 2.4 MB): domain_knowledge 1,465 · falsification 1,458 · deception_detection 1,411 · VERIFIED 397 · DECEPTION 399 · ANALYSIS 397 · STUB_DETECTION 223 · KNOWLEDGE 106 · POSITIVE/NEGATIVE/TOOL_USAGE 75 each · science/mathematics/logic/ethics/corporate **4–5 each — the actual knowledge-category gap to fill**. **`canonical_sft_v2.jsonl` (this session) = v1 + 1,146 arxiv reasoning pairs → 7,373 unique rows, 0 dedup collisions (via `merge_arxiv_into_canonical.py`, same canonical dedup scheme): mathematics 147 (was 4), logic 137 (was 4), science 83 (was 5), domain_knowledge 2,257 — the thin knowledge categories are measurably filled.**

**Deterministic generators (training-data factories):**
- `generators/pr25_synthetic_dataset.py` — deterministic LoRA SFT data generator (root seed `OE_PR25_ALPHA_OMEGA_LORA`, 7-layer topology, sharded) — no randomness, hash-verifiable.
- `generators/` 1M–1Qi LOC fractal generator — code-domain corpora at any scale, Merkle-verifiable (verified this session: 1M DAG + 1Qi multiverse checks pass; `batch_materializer.py` fixed for Python 3.12).
- `arxiv_vendor/` — arXiv ingestion DAG → materializer → Merkle builder (paper-scale knowledge corpus pipeline). **Measured this session: 1,200 papers (12 categories × 100), metadata-only, 2.2 MB on disk, Merkle-anchored (12 per-category roots + grand root `2ebfa653…c1bb2a` verified in `merkle_roots/arxiv_grand_root.json`, SHA-256 per paper).** Category mix is reasoning-heavy — cs.LO, math.LO, math.CT, cs.FL, stat.ML, cs.AI/safety (e.g. an LLM-harmfulness causal-intervention paper in cs.AI) — the natural feedstock for the HLE gap. Full text stays lazy (`lazy_materialization: true`, no PDFs stored); title/abstract metadata materializes into SFT pairs via `arxiv_materializer.py`.
- **`arxiv_reasoning_pairs.py` (new this session) — the HLE-gap data pipeline, built & verified:** converts the 1,200 on-disk abstracts into canonical-schema reasoning pairs deterministically (regex extraction only, no LLM): **1,146 pairs from 1,090 unique papers** (110 cross-category dupes dropped) — mathematics 143 (119 A + 24 B), logic 133 (101 A + 32 B), science 78, domain_knowledge 792. Template A = 4-part verbatim claim decomposition (problem/method/result/limit) on all papers; Template B (math+logic) = falsification-condition extraction, **emitted only when the abstract states a real limitation** (marker-only rows collapse under the canonical `sha256(instruction|output)` dedup key — constant in, constant out — so they are dropped at generation, not at merge). **Determinism verified: sha256 `e1226c53…` identical across two runs; every row carries `_row_sha256` + canonical `dedup_group`.** Honest limits: LIMIT coverage 33.9% (A) / 25.5% (B); seed-scale structured claim data, not generated prose.
- `canonical_evidence/` — curated evidence sets: ai_interaction_patterns, boundary_enforcement, mathematical_proofs, claude_analysis.
- Workspace domain modules (knowledge): shampoo-ontology-v4/v5 (parser, supplier audit, diagnostics — 100/100), truthsystems-mod, ftb-quests-ontology, standardgalactic-*.

## 2. Eval layer

- `benchmarks/` — `oe_benchmark_suite.py`, `capability_matrix.py`, `run_arc_benchmark.py`, `model_profiles/`, `MODEL_PERFORMANCE_REGISTRY.md`, `KIMI_PERFORMANCE_REGISTRY.md`.
- **`benchmarks/model_profiles/` — 11 cross-model profiles (deepseek_v3, claude_opus_4_5, gemini_3_pro, gpt5, grok_3, kimi_k2_5, llama_4, mistral_large, qwen_3, cohere_command_r, devin) with benchmark scores + documented failure modes — ready-made target-setting registry.** V3.2 profile highlights: ARC-AGI-3 = 0.0 (no V4 number published → measure in-cycle), BrowseComp 0.412 (fixed in V4: 73.2/83.4), confabulation incidents (PR #81) → alignment targets.
- **`CROSS_MODEL_TARGET_MATRIX.md` (new this session) — cross-model matrix extracted from all 11 profiles (via `extract_profiles.py`). Two decisive findings: (1) ARC-AGI-3 = 0.0 for *every* profile — a universal frontier, not a DeepSeek regression; first team to publish a real V4 number sets the reference. (2) The biggest measured gap is HLE: Kimi K2.5 0.502 / Gemini 3 Pro 0.458 / GPT-5.2 0.455 vs V4-Flash max 0.348 (≈15 pts → target 0.40+).** Also: BrowseComp Flash 0.732 vs Kimi 0.749 (Pro 0.834 already ahead of field — distill down to Flash), LiveCodeBench V4 0.916/0.935 already the field leader (hold, don't regress), SWE mid-pack 0.790 → 0.83+ target. Registry caveat: profiles and V4 READMEs use different eval versions — re-baseline on one harness first (checklist item).
- **`benchmarks/run_arc_benchmark.py` + `axioms/arc_solver.py` — VERIFIED RUNNING this session:** symbolic ARC solver (not an LLM eval), 10/10 demo tasks pass with Merkle-anchored proofs — the verification half of a hybrid eval (LLM proposes, solver verifies) and a working reference for ARC-AGI-3 in-cycle measurement.
- **`DEEPSEEK_V3_TO_V4_DELTA.md`** — config lineage V3.2→V4 (61/43/61 layers; routing 8/256→6/256; indexer top-k 2048→512/1024; context 160K→1M; KV claim check: paper's "10%" reproduces only for Flash-vs-bf16-V3.2 (8%); Pro measures 20–40% — flagged for re-derivation).
- New this session (reusable): `scale_token_test.py` (token economics, any tokenizer, any corpus), `probe_real_repos.py` (real-code probe), BRR harnesses (small-model recursive-refinement eval), `measure_qwen_speed.py` (local throughput).

## 3. Tooling layer — built and validated this session

**`expert_census.py` — DRY-RUN VERIFIED against the 72,317-tensor index:** 43 layers × 256 experts, 46 gate tensors (incl. 3 MTP), 3 hash-bootstrap layers, schema ✓. Full run `[needs hardware]`: hooks the TopKRouter, counts (layer, expert) activations + gate scores over a code corpus, emits the coldest-30% prune candidate list with exact tensor names (`layers.N.ffn.experts.E.w{1,2,3}.weight`). Corpus format: `--corpus` accepts a .py dir or .jsonl of `{"code": ...}`.

**Tokenizer experiments — measured, one hypothesis falsified:**
- V4 tokenizer on code: 3.69 c/t (generated, 361K lines), **3.745 c/t (real repos, 154 files)**, 3.649 (benchmarks src). V4 beats Qwen by 25.6% on code.
- From-scratch code BPE (18.6 MB training, 129,280 vocab): **did NOT beat V4** — benchmarks src 3.649 → 4.024 (+10% worse), generated ~equal. → **U1 update: tokenizer retrain-from-scratch is falsified at this scale; the remaining path is vocab continuation** (add code merges on top of V4's existing vocab, freeze the base) — small, well-defined experiment for the team.
- Headline context fact (measured): 1M-token context ≈ 3.7M chars ≈ **~105K LOC** — repo-scale engineering is a context-engineering problem, not a model-size problem.

**Local-density baselines (measured):** qwen2.5-1.5B fp16 = 39.7 tok/s on RTX 4050; V4-Flash-0731 = 158 GB weights / 166.9 GB storage, 82.7 Terminal Bench 2.1, density 0.50 %/GB (vs Pro 1.6T/865 GB ≈ 0.10).

## 4. Category × asset matrix (use for the next cycle)

- **Agentic / tool-use**: TOOL_USAGE pairs (v4–v7), generator corpora as tool-call fixtures, Terminal Bench 2.1 82.7 → 90+ target; MCPAtlas 73.8 hold.
- **Coding**: generator code corpora (any scale, deterministic), code-tokenizer continuation experiment, DSpark-for-code accept-rate test `[needs hardware]`.
- **Math/logic**: mathematics 188 + logic 218 entries per file, canonical mathematical_proofs, arxiv_vendor papers; Codeforces 3052 → 3200+ target.
- **Long context**: arxiv corpus + generator corpora for 1M-ctx evals (MRCR-1M 78.7 → 83+); KV runtime probe `[needs hardware]`.
- **Knowledge**: domain_knowledge (2,930×4), governance pairs (200), domain modules (shampoo ontology etc.), arxiv_vendor pipeline.
- **Alignment/safety**: falsification 2,930 + deception_detection 2,930 per file, POSITIVE/NEGATIVE + DECEPTION/VERIFIED pairs, STUB_DETECTION, canonical_evidence/boundary_enforcement; `verify_*` gates as post-training checks.
- **Efficiency**: expert census → prune → INT2 path (158 → ~55–80 GB target); density metric 0.50 → 0.8–1.0 %/GB.

## 5. Next-cycle checklist

1. **Dedupe + merge** v4–v7 into one canonical SFT/preference dataset (schema: instruction/input/output/category; add `verified` flag).
2. **Run expert census** on 4×GPU with a code+agentic corpus (generator output + SWE-bench) → prune candidates.
3. **Tokenizer continuation experiment** (vocab extension on V4 base, target ≥5% token cut on code).
4. **DSpark accept-rate + KV runtime probes** on big hardware (scripts patterned on this session's harnesses).
5. **Wire the eval suite**: capability_matrix + oe_benchmark_suite + token harness against each training milestone (registry files updated per run).
6. Everything above is reproducible from `oe-local/2026-08-04/` scripts + the generator — no external dependencies.

## 6. Honest gaps

- No V4 runtime on this machine: census/DSpark/KV items are `[needs hardware]`; scripts are dry-run-validated only.
- Preference pair counts are small (150–200/category); v8 governance set is 200 entries — treat as seed data, not corpus.
- Tokenizer retrain-from-scratch measured and falsified; the continuation path is untested (defined, small).

## 7. Artifacts (oe-local/2026-08-04/)

`expert_census.py` (dry-run ✓) · `scale_token_test.py` · `probe_real_repos.py` · `train_code_tokenizer.py` + `code_tokenizer_v1.json` (baseline artifact) · `measure_qwen_speed.py` · `_inventory_data.py` · `extract_profiles.py` (matrix extractor, re-runnable) · `CROSS_MODEL_TARGET_MATRIX.md` (this session) · verified ARC solver run (10/10, Merkle proofs) · arxiv_vendor quantification (1,200 papers, grand root verified) · `arxiv_reasoning_pairs.py` + `arxiv_reasoning_pairs.jsonl` (1,146 deterministic pairs, sha-verified) · `merge_arxiv_into_canonical.py` + `canonical_sft_v2.jsonl` (7,373 rows, 0 dupes) · **`V4_EDGE_CASE_RESOLUTIONS.md` (12-hypothesis deck; #6 MEASURED, #12 Ortyx-derived)** · `v4_bootstrap_load_probe.py` · `tokenizer_continuation_probe.py` (**continuation headroom MEASURED: −9.4% real-code tokens @1K merges, −17.5% @10K**) · `tokenizer_continuation_candidates.py` + `tokenizer_continuation_candidates.jsonl` (**top-20,000 merge pairs, 3.0 MB bytes-saved — the V5-1 apply-file**) · `HLE_HARNESS_SPEC.md` (pre-registered eval contract, hardware-gated) · **`hle_item_synthesizer.py` + `hle_items_dev.jsonl` (HLE dev set MATERIALIZED: 56 verifier-keyed falsification items, sha `6eb5092c…`, 0 failures)** · **`run_hle.py` (runner: score mode verified on mock 9/10; decode hardware-gated) · `dataset_quality_probe.py` + `dataset_quality.json` (v2 hygiene: 62.8% template diversity, 0 empty outputs, 39.6% empty inputs — schema note) · **`expert_affinity_probe.py` (extension-rule sim: hash self-balancing 1.28×, pin-fix for base table only) · `merge_order_check.py` (BPE property gate: 22 multi-step pieces + 5,678 ordering constraints flagged) · `margin_stream_collector.py` + `pll_jitter_sim.py` (aerospace PLL gate: code −18.5% draft cost, reasoning 100% locked) · `locgen7_MANIFEST.json` (1B-corpus chain root workspace-verifiable) · **`tokenizer_continuation_apply.py` + `tokenizer_continuation_apply_v1.jsonl` (ordered apply-file: 20,000 rows, 0 order failures) · `kv_decay_schedule.py` + `kv_decay_schedule.json` (decay solved: 2M→53% / 4M→25% CSA retention for 6.5 GB)** · `registry_normalize.py` + `registry_normalized.json` (**E4 fix: version-risk-flagged cross-model matrix**) · **`V4_EDGE_CASE_RESOLUTIONS.md` now 13 hypotheses (#13 wave-order preservation — order-preserving thinning for the decay schedule)** · **`tid2eid_extension_table.py` + `tid2eid_extension_v1.jsonl` (loadable extension table: 20,000 entries, ids 129,280–149,279, top-100 pinned, assertions passed) · `WORK_LOG.md` (oe-local root — commits-history, instant recency answer)****** · `jitter_gate_feasibility.py` (**#12 gate signal measured on qwen-1.5b: domain prior FALSIFIED — tail-based gate design**) · `generator_edge_sweep.py` + `generator_edge_sweep.json` · **`GENERATOR_SCALE_TEST.md` (2 toolchain bugs fixed in generators/: max_depth str-vs-int + seed invariant; 1B tokens ≈ 20–25 min locally, chain-rooted)** · `stub_placeholder_scan.py` + `stub_placeholder_scan.json` (**stub/placeholder audit: 1 real stub fixed in benchmarks/ai_invariant_tests.py; 5 benign — re-run after new work**) · **`NEXT_CYCLE_LEARNING.md` (decision-ready: D1–D5 data anchors, E1–E4 eval lessons, 7-item experiment order, 5 falsified paths; 10-batch corpus 122 MB chain root `aafd3c35…`)** · `scale_run_300.py` (**lazy single-batch DAG mode — eager-DAG ceiling solved, byte-identical verified; 300-batch ≈1B-token run COMPLETED: 3.73 GB, chain root `a07920a6c404…`, `/tmp/locgen7/`**) · `tokenizer_continuation_candidates_v2.jsonl` (**clean-real-code axes: 86% of top-20K supported by hvac-simulation/standardgalactic, 13 three-axis safest**) · **`CHAIN_OF_CUSTODY.md` (oe-local root — master entry point: scope, custody, resume order)** · reports: `DEEPSEEK_V4_ARCHITECTURE_INVESTIGATION.md`, `V4_UPGRADE_SPEC_FOR_SOFTWARE_USE.md`, `V4_TO_V5_GLOBAL_TRAJECTORY.md` · fixed: `generators/batch_materializer.py`.
