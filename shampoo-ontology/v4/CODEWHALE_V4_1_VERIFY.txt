You have the v4.1 files at /home/idor/shampoo-ontology-v4/. The 3 bugs are fixed in code but the diagnostics engine may still report stale scores because its test cases and scoring weights haven't been synced to the fixes.

DO NOT build FastAPI, Flask, automated testing harnesses, or v5.0 blueprints. Those are aspirational filler. The v4 instructions explicitly reject "future integration" and "could be extended" language.

What you actually need to do:

STEP 1: Verify the fixes are in the files
Run this bash command to check:

grep -n -E 'Aqua|AQUA|aqua' /home/idor/shampoo-ontology-v4/shampoo_ontology_parser.py | head -5
grep -n '\.upper()' /home/idor/shampoo-ontology-v4/shampoo_ontology_supplier_audit.py | head -5
grep -n -E 'BOTANICAL_FALLBACK|botanical_fallback' /home/idor/shampoo-ontology-v4/shampoo_ontology_parser.py | head -5

If any fix is missing, apply it. If all are present, proceed.

STEP 2: Run the diagnostics module and capture the actual score

cd /home/idor/shampoo-ontology-v4 && python3 shampoo_ontology_diagnostics.py

If the score is not 100.0/100, the diagnostics engine's scoring algorithm or test cases need adjustment. Common causes:
- The botanical fallback generates canonical names like "BOTANICAL_EXTRACT_EXTRACT_APPLEFRUIT" which may be flagged as malformed. Fix the regex to produce clean names like "BOTANICAL_EXTRACT_APPLE_FRUIT".
- The diagnostics engine may have hardcoded expected scores or test data that doesn't match the updated parser. Update the test data to use the new canonical names.
- The "Aqua" test case in the diagnostics engine may still expect "Aqua" to be flagged as an orphan. Update it to expect "WATER".

STEP 3: Fix any remaining scoring mismatches
Edit the diagnostics engine's test cases and scoring weights to match the fixed parser behavior. The goal is 100.0/100 with zero flagged issues.

STEP 4: Run the orchestrator end-to-end

cd /home/idor/shampoo-ontology-v4 && python3 shampoo_ontology_orchestrator.py

Verify it produces valid JSON output and no exceptions.

STEP 5: Copy final files to Windows

mkdir -p "/mnt/c/Users/Aidor/Downloads/add to oe_ shampoo/v4_output"
cp /home/idor/shampoo-ontology-v4/*.py "/mnt/c/Users/Aidor/Downloads/add to oe_ shampoo/v4_output/"
cp /home/idor/shampoo-ontology-v4/*.html "/mnt/c/Users/Aidor/Downloads/add to oe_ shampoo/v4_output/" 2>/dev/null || true
cp /home/idor/shampoo-ontology-v4/*.txt "/mnt/c/Users/Aidor/Downloads/add to oe_ shampoo/v4_output/" 2>/dev/null || true
ls -la "/mnt/c/Users/Aidor/Downloads/add to oe_ shampoo/v4_output/"

STEP 6: Report exact status
Report: (a) which fixes were verified, (b) final diagnostics score, (c) number of remaining issues, (d) file sizes of all outputs, (e) confirmation that files were copied to Windows.

No new tools needed. No web frameworks. No testing harnesses. Just verify, fix scoring mismatches, run, copy, report.